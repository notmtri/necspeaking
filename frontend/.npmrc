# react-scripts@5 pins typescript@4, while some transitive OPTIONAL peers
# (e.g. @vercel/analytics -> @sveltejs/kit) ask for typescript@5. Nothing here
# actually uses Svelte or TypeScript, so the conflict is spurious. npm still
# refuses to install without this, so keep it until the app moves off CRA.
legacy-peer-deps=true
